# Changes — 2026-07-07

## Headline

Reframed the repo from an **HR-flavored fullstack app** into a **generic AI-agent
boilerplate skeleton**. It now ships **no application code** — the value is the
`.claude/` agent layer + memory + token-saving proxy + build tooling for 5 targets
(web, PWA, APK/AAB, IPA, EXE).

**Everything this session is UNCOMMITTED** (per user instruction). HEAD is at the
last pushed commit `80ef1bc`; all changes below are working-tree edits. Backups of
the original schema/seed/permissions/enums are in `memory/backups/`.

---

## Git

- Uncommitted the two local-only v2 commits (`9bbae37` Phase 1, `d93bb79` Phase 2)
  via `git reset 80ef1bc` (mixed — no file changes lost). Nothing ahead of origin.
- The earlier pushed audit commits (up to `80ef1bc`) are untouched on GitHub.

---

## Kept (the boilerplate = agent layer + tooling)

- `.claude/` — all agents, skills, rules, commands, hooks, `mcp.json` + catalog,
  pxpipe proxy docs, design-first / build-loop / verify-wired / graph commands
  (from the uncommitted Phase 1–5 work).
- `memory/` — full memory system.
- Build tooling for all 5 targets: Vite + PWA/Workbox, Capacitor (APK/AAB/IPA),
  Electron/`agent-desktop` (EXE), Nginx, Docker, CI/CD, PM2.
- Cross-cutting backend infra: `config/env`, `lib/{prisma,redis,logger}`,
  `middleware/{errorHandler,rateLimiter,requestId,requestLogger,validation}`,
  `utils/encryption`.
- Frontend design system: all `components/ui/*` primitives, layout shell,
  `globals.css`, Tailwind config, `sw.ts` (PWA), i18n, generic hooks.
- Generic shared contract: `types.ts` (ApiResponse/PaginationMeta) + `common.schema.ts`.

## Removed (all application / business / auth code)

- Backend: `modules/` (auth + dashboard), `services/`, `jobs/`, `sockets/`,
  `middleware/{auth.middleware,upload.middleware}`, `utils/{auditLogger,pdfGenerator,excelExporter}`,
  `config/swagger.ts`.
- Frontend: `features/` (auth + dashboard), `lib/socket.ts`, `hooks/useAuth.ts`.
- Shared: `permissions.ts`, `enums.ts`, `schemas/auth.schema.ts`.
- Prisma: all models (Organization/User/RefreshToken/Notification/AuditLog + earlier
  Employee/Department/Designation). Schema now ships **zero models** with a commented
  `Item` example. Seed is a no-op.
- e2e: `auth.spec.ts`, `dashboard.spec.ts` → replaced with `home.spec.ts` smoke test.

## Rewrote to skeleton

- `backend/src/server.ts` — boot only. `backend/src/app.ts` — health check only.
- `frontend/src/app/{store,api}.ts` — minimal store + tagless RTK Query base.
- `frontend/src/router/AppRouter.tsx` — single placeholder route.
- `frontend/src/features/home/HomePage.tsx` — new placeholder landing page.
- `frontend/src/hooks/useAppStore.ts` — new generic useAppDispatch/useAppSelector.
- `frontend/src/components/layout/{Sidebar,Topbar}.tsx` — stripped business nav/logout.
- `shared/src/{index,types}.ts` + `schemas/index.ts` — generic contract only.

## Added

- `docs/tech-stack-targets.md` — modern tech choice per target (web/PWA/APK/AAB/IPA/EXE).
- Fixed pre-existing `lib/redis.ts` ioredis import + moved the `Request.id` type
  augmentation into `requestId.ts` (was in the deleted auth.middleware).

## Debrand

- "Monday Aniston design system" → "Boilerplate Design System" across design files
  (globals.css, tailwind.config, rule-frontend, agent-ui-ux, agent-frontend-wiring,
  skill-ui-ux-checklist, settings.json). Company attribution (README/AGENTS/author)
  kept. Placeholder appIds unified to `com.example.boilerplate`.

## Verified

- `npm run db:generate` — Prisma client generates from empty schema.
- `npm run typecheck` — shared ✓, backend ✓, frontend ✓ (all clean).
- `npm run build --workspace=frontend` — builds incl. PWA service worker (6 precache
  entries, 417 KiB).

## Genericization + model fix (later same session)

- **Model field:** `claude-opus-4-7` → `opus` (alias, resolves to latest Opus 4.8, passes the
  agent-frontmatter validator that rejected the version string) across all 21 agents + settings.json.
- **Genericized all 72 shipped `.md` files** (workflow `w4acvtyue`, 73 agents, 3.4M tokens): every
  HR example (Employee/Department/Designation/Leave/Payroll/aadhaar/pan/bank, MANAGER/EMPLOYEE roles)
  → canonical generic vocabulary (`Item`/`Category`/`MEMBER`/`ownerId`/`secretEncrypted`,
  status DRAFT→SUBMITTED→APPROVED→REJECTED→CANCELLED). Final grep: **0 residual HR terms**.
- Cleaned `on-prompt.sh` HR keywords ("leave request", "employee module" comment).
- The two "NOT done" doc items below are now DONE (docs + skills genericized).

## NOT done (left for a follow-up if wanted)

- CLAUDE.md still lists HR-era skills/agents references in some tables (the skills
  themselves still use example domains — but they're teaching docs, not shipped code).
- `docs/{architecture,database-erd,api-conventions}.md` + `SOP-Fresher-Guide.md`
  still contain the old HR examples (docs only, no code impact).
- Skills that reference `Employee`/`Leave` in their example snippets were NOT
  rewritten (they teach patterns; no code ships). Rewrite to generic `Item` if desired.
