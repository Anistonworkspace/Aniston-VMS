# Changes — 2026-07-08

Implemented the **agent-power plan** ([_active/2026-07-08-agent-power-plan.md](../plans/_active/2026-07-08-agent-power-plan.md)).
All changes **UNCOMMITTED** (HEAD at pushed `80ef1bc`). Model field also fixed and all
72 `.md` docs genericized earlier this session (see the top of this file's sibling notes).

## A — Fable-grade persistence (finish to production, don't stop half-way)
- **`.claude/hooks/on-stop.sh`** — completion Stop-gate. When uncommitted `.ts/.tsx`
  changes contain incompleteness markers in ADDED lines (`TODO/FIXME/XXX`,
  `not implemented`, `// stub|placeholder`, backend `console.*`), it emits a Stop-hook
  `{"decision":"block","reason":...}` so Claude keeps going. Capped at 3 consecutive
  blocks; escape via `/stop-anyway` or `ALLOW_INCOMPLETE=1`. **Verified: blocks on a stub.**
- **`.claude/commands/stop-anyway.md`** — one-shot escape (touches `.claude/logs/.stop-anyway`).
- **`rule-completion-standards.md`** — hard "Definition of DONE" section.
- **CLAUDE.md** — new CRITICAL RULE #6 (finish to production).
- **Persistence directive** appended to agent-completion-loop, agent-planner,
  agent-test-writer, agent-frontend-wiring.

## B — Fewer tokens per prompt
- **`on-prompt.sh`** — dispatch now caps injected SKILLS to **top-3** (order-preserving,
  primary-intent-first). Verified ≤3. Also fixed dedup to preserve relevance order.
- **CLAUDE.md slimmed 369 → 238 lines** — moved the Agents/Skills/Rules catalog to new
  **`docs/reference-index.md`** (loads on demand, not every message).
- **pxpipe** verified available on npm (`pxpipe-proxy` v0.8.0) → `/proxy-start` works.

## C — Context capsule (survive compaction, cheap resume)
- **`.claude/hooks/pre-compact.sh`** — mechanical capsule writer: goal (from newest active
  plan), open next-steps, **Files-in-play with paths + role**, git status/diff, paste-to-resume
  block → `memory/sessions/compact/LATEST-capsule.md` (+ timestamped copy). **Verified.**
- **`PreCompact` hook** wired in settings.json → auto-writes the capsule before compaction.
  `on-stop.sh` also refreshes it each session.
- **`.claude/commands/handoff.md`** — Claude writes a richer, human-authored capsule.
- **`.claude/commands/resume.md`** — new chat reads the capsule + only the listed files
  (no repo search).

## D — Graphify (activated as far as the environment allows)
- **Python is NOT installed** on this machine, so the real Graphify (Python: Tree-sitter +
  NetworkX) can't run here, and the npm `graphify` is an unrelated charting library (name
  clash — not installed).
- Built **`.claude/scripts/graph.mjs`** instead — a zero-dep Node dependency-graph tool
  (`build`/`deps`/`inbound`/`explain`). **Verified: 43 files, 44 nodes, 58 edges; deps &
  inbound queries correct.** `.claude/graph/` gitignored.
- **`/graph`** command rewired to the Node tool; real Graphify documented as the Python upgrade.
- Headroom stays documented (`docs/upgrade-to-headroom.md`) as the deeper-compression upgrade
  (also Python; not wired).

## Files added
- `.claude/hooks/pre-compact.sh`, `.claude/scripts/graph.mjs`
- `.claude/commands/{handoff,resume,stop-anyway}.md`
- `docs/reference-index.md`
- `memory/sessions/compact/LATEST-capsule.md` (auto-generated)

## Graphify — NOW REALLY INSTALLED + WIRED (update, later 2026-07-08)
- User installed Python 3.14.6 + the real package **`graphifyy`** (double-y — plain
  `graphify` on PyPI is an unrelated charting lib; the name-clash is confirmed). It exposes
  a `graphify` module: run via **`python -m graphify`** (the `.exe` isn't on the Bash PATH).
- Verified on Python 3.14: full-repo build = **2686 nodes, 2564 edges, 296 communities**
  from 320 source files (skips node_modules). `explain "createApp"` returns source location
  + community + degree + inferred call edges. Richer than the Node tool.
- Ran **`python -m graphify install --platform claude`** → installed the official
  **`/graphify`** skill at user level (`~/.claude/skills/graphify/`) + `~/.claude/CLAUDE.md`.
- **Repointed `.claude/commands/graph.md`** to prefer `python -m graphify` (build via
  `update .`, `explain`, `path`) with the Node `graph.mjs` tool as the no-Python fallback.
- `graphify-out/` added to `.gitignore` (regenerable; git status stays clean).

## Still documented-only (needs its own install)
- Headroom (`headroom-ai`) — not installed; upgrade path in `docs/upgrade-to-headroom.md`.
- pxpipe not left running (starts on demand via `/proxy-start`).

## Commit cleanup - pre-commit hook
- Added root `package.json` `type: module` so Node treats `eslint.config.js` and `lint-staged.config.js` as ESM without typeless-package warnings.
- Changed `prisma/seed.ts` from `console.log` to `console.info` to satisfy the repo `no-console` policy under `--max-warnings=0`.
- Verified focused ESLint command: `npx.cmd eslint prisma/seed.ts --fix --max-warnings=0`.

## Setup Doctor + hard gate (approved plan 2026-07-08-setup-doctor-plan.md)
- **`.claude/hooks/doctor.sh`** — checks Node/Python/npm/graphifyy//graphify-skill/graph-fresh/
  handoff-wiring (REQUIRED) + pxpipe/Headroom (RECOMMENDED/OPTIONAL). Exit 0 if all required green,
  writes/removes `.claude/logs/.doctor-green`. Modes: check, `--fix` (auto-install safe pieces), `--quiet` (gating).
- **`on-prompt.sh`** — runs the doctor ONCE PER DAY (first prompt); green → 1-liner; not-green →
  injects the report + a HARD-GATE directive so Claude auto-fixes and won't build until green.
- **Hard gate** — `/build-loop` (Step precondition) + `/new-module` (Step 0) run `doctor.sh --quiet`
  and refuse to scaffold unless it exits 0. pxpipe/Headroom are warn-only (never block).
- **`/doctor`** command (+ `--fix`); **`scripts/setup-ai-tools.sh` + `.ps1`** one-command idempotent
  installer; **`docs/NEW-MACHINE-SETUP.md`** full clone→build guide; `package.json` gains
  `setup:ai`, `setup:ai:win`, `doctor`, `doctor:fix`; CLAUDE.md CRITICAL RULE #6 (doctor gate).
- Verified: --quiet exits 0 green / 1 when a required tool is hidden (marker dropped → gate blocks);
  daily green 1-liner fires; configs valid.
